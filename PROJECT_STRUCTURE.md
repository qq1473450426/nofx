# NOFX AI Trading System - 项目架构文档

**版本**: V2.0 (优化后)  
**更新日期**: 2025-11-08  
**架构**: Multi-Agent预测驱动架构

---

## 📁 项目目录结构

```
nofx/
├── 📄 核心文档
│   ├── README.md                    # 项目介绍（多语言）
│   ├── START_HERE.md               # 快速开始指南
│   ├── QUICK_START.md              # 快速部署文档
│   ├── CODEBASE_OVERVIEW.md        # 代码库概览
│   ├── 使用说明.md                  # 中文使用说明
│   ├── 常见问题.md                  # 常见问题解答
│   └── PROJECT_STRUCTURE.md        # 本文档
│
├── 🔧 管理脚本
│   ├── start.sh                    # Docker启动脚本
│   ├── stop.sh                     # 停止系统
│   └── status.sh                   # 查看状态
│
├── ⚙️  配置文件
│   ├── config.json                 # 主配置文件
│   ├── config.json.example         # 配置模板
│   ├── Dockerfile                  # Docker镜像
│   ├── docker-compose.yml          # Docker编排
│   └── .env.example                # 环境变量模板
│
├── 📦 核心代码 (Go)
│   ├── main.go                     # 程序入口
│   │
│   ├── config/                     # 配置管理
│   │   └── config.go
│   │
│   ├── decision/                   # 决策引擎 ⭐核心
│   │   ├── engine.go              # 决策引擎入口
│   │   │
│   │   ├── agents/                # Multi-Agent架构
│   │   │   ├── orchestrator.go                  # 协调器（单一Prompt备份）
│   │   │   ├── orchestrator_predictive.go       # 协调器（Multi-Agent主架构）
│   │   │   ├── regime_agent.go                  # 市场体制Agent
│   │   │   ├── signal_agent.go                  # 信号检测Agent
│   │   │   ├── position_agent.go                # 持仓管理Agent
│   │   │   ├── risk_agent.go                    # 风险计算Agent
│   │   │   ├── prediction_agent.go              # 预测Agent ⭐优化后
│   │   │   ├── market_intelligence.go           # 市场情报Agent
│   │   │   └── utils.go                         # 工具函数
│   │   │
│   │   ├── tracker/                             # 预测跟踪器
│   │   │   ├── prediction_tracker.go            # 预测记录与评估
│   │   │   └── cmd/prediction_stats/main.go     # 统计工具
│   │   │
│   │   └── types/                               # 类型定义
│   │       └── prediction.go                    # 预测相关类型
│   │
│   ├── market/                     # 市场数据
│   │   ├── data.go                # 数据获取
│   │   └── extended_data.go       # 扩展数据（清算、资金费率等）
│   │
│   ├── trader/                     # 交易执行
│   │   ├── auto_trader.go         # 自动交易器
│   │   ├── binance_futures.go     # Binance期货接口
│   │   ├── mock_trader.go         # 模拟交易器
│   │   └── constraints.go         # 硬约束管理
│   │
│   ├── manager/                    # 管理器
│   │   └── trader_manager.go      # 交易器管理
│   │
│   ├── logger/                     # 日志系统
│   │   ├── decision_logger.go     # 决策日志
│   │   └── cycle_logger.go        # 周期日志
│   │
│   ├── mcp/                        # AI接口
│   │   └── client.go              # MCP客户端
│   │
│   └── api/                        # REST API
│       └── server.go              # Web API服务器
│
├── 🌐 前端 (React + TypeScript)
│   └── web/
│       ├── src/
│       │   ├── components/        # React组件
│       │   ├── hooks/            # 自定义Hooks
│       │   └── App.tsx           # 主应用
│       ├── package.json
│       └── vite.config.ts
│
├── 📊 数据目录
│   ├── logs/                      # 运行日志
│   │   └── nofx.log
│   │
│   ├── decision_logs/             # 决策日志（按trader分类）
│   │   └── [trader_id]/
│   │       └── decision_*.json
│   │
│   └── prediction_logs/           # 预测日志
│       └── [SYMBOL]_[timestamp].json
│
├── 🧪 测试
│   └── test_*.go
│
└── 📦 依赖管理
    ├── go.mod
    └── go.sum
```

---

## 🏗️ 核心架构

### **1. Multi-Agent决策系统** ⭐最新

```
决策流程 (orchestrator_predictive.go):
┌────────────────────────────────────────────────────────────────┐
│ STEP 1: 市场情报收集 (MarketIntelligenceAgent)                  │
│   输入: BTC市场数据 + 候选币种列表                                │
│   输出: MarketIntelligence {phase, risks, opportunities}        │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│ STEP 2: 持仓管理 (PredictionAgent → PositionAgent)              │
│   对每个持仓:                                                     │
│     1. PredictionAgent.Predict() → 预测未来走势                  │
│     2. shouldClosePosition() → 判断是否平仓                      │
│   输出: close/hold决策                                           │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│ STEP 3: 寻找新机会 (PredictionAgent)                            │
│   对每个候选币种:                                                 │
│     1. PredictionAgent.Predict() → AI预测                       │
│     2. 筛选: probability ≥ 阈值 && confidence符合要求             │
│   输出: 有效预测列表                                              │
└────────────────────────────────────────────────────────────────┘
                              ↓
┌────────────────────────────────────────────────────────────────┐
│ STEP 4: 风险计算与仓位分配 (RiskAgent)                          │
│   对每个有效预测:                                                 │
│     1. calculatePositionFromPrediction() → 凯利公式计算仓位       │
│     2. validateRiskParameters() → 风控验证                       │
│   输出: Decision {action, leverage, stopLoss, takeProfit}        │
└────────────────────────────────────────────────────────────────┘
                              ↓
                        最终决策列表
```

### **2. PredictionAgent架构** ⭐优化后 (V2.0)

```go
// prediction_agent.go

// 核心功能：基于市场数据预测未来走势
type PredictionAgent struct {
    mcpClient *mcp.Client
}

// 预测流程
func (agent *PredictionAgent) Predict(ctx *PredictionContext) (*types.Prediction, error) {
    // 1. 数据验证
    validateMarketData(ctx)
    
    // 2. 构建Prompt（V2优化版）
    systemPrompt, userPrompt := buildPredictionPrompt(ctx)
    
    // 3. 调用AI API
    response := mcpClient.CallWithMessages(systemPrompt, userPrompt)
    
    // 4. 解析JSON
    prediction := parseJSON(response)
    
    // 5. 标准化
    normalizePrediction(prediction)
    
    // 6. 概率校准（可选）
    calibrateProbability(prediction, ctx)
    
    // 7. 验证
    validatePrediction(prediction)
    validatePredictionEnhanced(prediction, md)
    
    return prediction
}
```

#### **关键优化点** (V2.0)

| 优化项 | 之前 | 之后 | 效果 |
|--------|------|------|------|
| **System Prompt逻辑** | <70%→neutral且neutral≤65% | <58%→neutral且neutral∈[50-58%] | ✅ 修复逻辑死锁 |
| **顺势交易规则** | 无 | 上升趋势只预测up/neutral | ✅ 减少逆势错误 |
| **概率校准规则** | 无 | 单一信号55-60%，多重共振70-80% | ✅ 防止过度自信 |
| **异常值检查** | 允许180%、580% | 限制±10%/±15% | ✅ 防止极端预测 |
| **置信度级别** | 5级 | 3级(high/medium/low) | ✅ 提高可用性 |
| **趋势验证** | RSI>80即拦截 | 只拦截高概率(>70%)逆势 | ✅ 平衡风控与灵活性 |
| **时间框架** | 99%是4h | 1h/4h/24h智能选择 | ✅ 根据波动率调整 |

---

## 📊 数据流

### **市场数据 → AI预测 → 风险计算 → 交易执行**

```
1. 市场数据采集
   ├─ Binance API → K线、指标、资金费率
   └─ 计算: EMA20/50/200, RSI7, MACD, ATR14

2. AI预测 (PredictionAgent)
   ├─ Input: MarketData + Intelligence + HistoricalPerf
   ├─ AI模型: Qwen/DeepSeek
   └─ Output: Prediction {direction, probability, expected_move, ...}

3. 风险计算 (RiskAgent)
   ├─ 凯利公式计算仓位
   ├─ 杠杆调整（基于风险级别）
   └─ 止损止盈计算

4. 交易执行 (BinanceFutures/MockTrader)
   ├─ 硬约束检查: 冷却期、日交易上限、仓位数量
   ├─ 下单执行
   └─ 动态止损管理
```

---

## 🔑 核心配置

### **config.json**

```json
{
  "traders": [{
    "id": "binance_live_qwen",
    "name": "Binance Live - Qwen Prediction",
    "enabled": true,
    "ai_model": "qwen",
    "exchange": "binance",
    "binance_api_key": "YOUR_API_KEY",
    "binance_secret_key": "YOUR_SECRET_KEY",
    "initial_balance": 1000.0,
    "scan_interval_minutes": 5
  }],
  "leverage": {
    "btc_eth_leverage": 10,
    "altcoin_leverage": 8
  },
  "default_coins": ["BTCUSDT", "ETHUSDT", "SOLUSDT"],
  "max_daily_loss": 10.0,
  "max_drawdown": 20.0
}
```

---

## 🚀 使用指南

### **快速启动**

```bash
# 1. 配置
cp config.json.example config.json
# 编辑config.json，填入API密钥

# 2. 编译
go build -o nofx

# 3. 运行
./nofx

# 4. 查看日志
tail -f logs/nofx.log
```

### **查看预测统计**

```bash
# 评估所有预测
go run decision/tracker/cmd/prediction_stats/main.go

# 只看特定币种
go run decision/tracker/cmd/prediction_stats/main.go -symbol=BTCUSDT

# 查看最近72小时
go run decision/tracker/cmd/prediction_stats/main.go -since=72h
```

---

## 📈 性能指标

### **当前系统表现** (基于390条历史数据)

| 指标 | 值 | 评价 |
|------|-----|------|
| 方向命中率 | 40.77% | ⚠️ 需提升到50%+ |
| 平均准确度 | 12.38% | ⚠️ 幅度预测不准 |
| AI平均概率 | 76% | - |
| 概率偏差 | -35.2% | ⚠️ AI过度自信 |

### **优化目标** (V2.0实施后)

| 指标 | 当前 | 目标 | 改进 |
|------|------|------|------|
| 方向命中率 | 40.77% | **50-55%** | +10-15% |
| 概率偏差 | -35.2% | **-15%以内** | 改善20% |
| very_high命中率 | 20% | **移除该级别** | 消除低质量预测 |

---

## 🛠️ 开发工具

### **代码规范**

```bash
# 格式化
go fmt ./...

# 测试
go test ./...

# 构建
go build -o nofx
```

### **日志位置**

```
logs/nofx.log                          # 主日志
decision_logs/[trader_id]/             # 决策日志
prediction_logs/                       # 预测日志（带评估结果）
```

---

## 🔐 安全注意事项

1. ⚠️ **永远不要提交config.json到Git**
2. ⚠️ **使用只读API密钥进行回测**
3. ⚠️ **实盘前在模拟环境充分测试**
4. ⚠️ **设置合理的max_daily_loss和max_drawdown**

---

## 📚 相关文档

- [START_HERE.md](START_HERE.md) - 新手快速开始
- [QUICK_START.md](QUICK_START.md) - 快速部署指南
- [CODEBASE_OVERVIEW.md](CODEBASE_OVERVIEW.md) - 代码库详细说明
- [使用说明.md](使用说明.md) - 中文使用手册
- [常见问题.md](常见问题.md) - FAQ

---

## 🤝 贡献

欢迎提交Issue和Pull Request！

---

**最后更新**: 2025-11-08  
**维护者**: NOFX Team  
**许可证**: MIT
